'''
@author: Gurpreet Singh
'''
def startDevice():
        
    # Example code to turn on a Phillips Hue Lamp -- Add logic to start your own IoT device
    # Github repo for phue: https://github.com/studioimaginaire/phue
    
#     from phue import Bridge
#     b = Bridge('ip_of_your_bridge')
#     
#     # If the app is not registered and the button is not pressed, press the button and call connect() (this only needs to be run a single time)
#     b.connect()
#     
#     # Get the bridge state (This returns the full dictionary that you can explore)
#     b.get_api()
#     
#     # Turn on lamp
#     b.set_light(1,'on', True)
#     
#     # Set brightness of lamp 1 to max
#     b.set_light(1, 'bri', 254)

    print("Device started")
    

def stopDevice():
    
    # Example code to turn off a Phillips Hue Lamp -- Add logic to stop your own IoT device
    # Github repo for phue: https://github.com/studioimaginaire/phue
    
#     from phue import Bridge
#     b = Bridge('ip_of_your_bridge')
#     
#     # If the app is not registered and the button is not pressed, press the button and call connect() (this only needs to be run a single time)
#     b.connect()
#     
#     # Get the bridge state (This returns the full dictionary that you can explore)
#     b.get_api()
#     
#     # Turn off lamp
#     b.set_light(1,'on', False)

    print("Device stopped")  
    
